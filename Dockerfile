FROM python:3.10-alpine
WORKDIR /code
# Copy todos los archivos incluidos el .env al work dir de python 
COPY      
RUN 
# despue sde terminar el archivo de docker que ya es todo solo pones en terminal docker build y start y docker run 